/*
package com.workshop.zarif.projection;

import org.springframework.data.rest.core.config.Projection;


@Projection(name = "customRole", types = Role.class)
public interface CustomRole {
    Integer getId();
    RoleName getRoleName();
}

*/
